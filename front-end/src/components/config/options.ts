import type { FitViewOptions, DefaultEdgeOptions } from '@xyflow/react';

export const fitViewOptions: FitViewOptions = { padding: 0.2 };
export const defaultEdgeOptions: DefaultEdgeOptions = { animated: true,
  type: 'default',
  style: { stroke: '#888' },
  labelStyle: { fill: '#555', fontSize: 12 }, };
